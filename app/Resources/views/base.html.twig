<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>

{% if photo.titre %}
	<img src="{{ asset(photo.getPath()) }}"/>
{% else %}
	Pas de photo chargée
{% endif %}

<h1>Charger photo</h1>
<form action="#" method="post"{{  form_enctyper(form) }}>
	{{ form_widget (form) }}
	
	<input type="submit" value="Uploader" />
</form>
